# Instructor Solutions

These are reference solutions for the mock interviewer. Keep this directory hidden from the candidate during a live mock interview.

To use a solution, copy the function body into the matching file in `src/`, then run:

```bash
pytest
```
