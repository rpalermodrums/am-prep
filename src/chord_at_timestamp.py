"""
Problem 2: Chord at Timestamp

Read the full prompt in problems/02_chord_at_timestamp.md.
"""


def chord_at(changes: list[tuple[int, str]], time: int) -> str | None:
    """
    Return the chord active at the given timestamp.

    Args:
        changes: Sorted list of (start_time, chord_symbol) pairs.
        time: Timestamp to query.

    Returns:
        The active chord symbol, or None if the time is before the first chord.
    """
    # TODO: implement this function.
    raise NotImplementedError
